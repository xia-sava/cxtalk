---
name: cxtalk
description: 同一マシンで並行して動く別の Claude Code セッションと直接会話する。別のリポジトリを担当するセッションに設計判断や仕様を確認したいとき、人間の仲介なしで往復するために使う。ルームに未読があると知らされたときもこれに従う。
---

# cxtalk

別のセッションと直接やりとりするためのツール。
人間が両者の間に立って伝言を運ぶ手間をなくす。

## 使う場面

開く価値があるのは、**相手の答えがないと自分の側を決められない**とき。

- 相手側の実装や制約を知らないと判断できない
- 双方の見解を突き合わせないと選べない
- 同じ話題で人間が何度も伝言している

開かなくてよいのは次の場合。

- 自分の側だけで決められる
- 伝えるだけで応答が要らない
- 相手のコードを読めば分かる

ルームを開くと人間に参加を依頼することになる。
自分の判断だけで開かず、ユーザーの依頼があるか、
ユーザーに提案して同意を得てから開く。

## 進め方

すべてのコマンドが `next` を返す。**次の行動は `next` に従う。**
`say` は発言、`receive` は待機、`report` は要約して報告、`ask_user` はユーザーに尋ねる。
`retry` だけは自分で直せる誤りなので、`hint` に従って直し、同じコマンドを呼び直す。

`ok: false` でも実行の失敗ではない。`next` が次の手を示しているので、
独自の回復動作を始めず、示されたとおりに進める。

`corrupt_room` / `unwritable` / `invalid_home` / `unexpected_failure` は会話する側では
直せない。**hint をそのままユーザーに渡す。** 勝手に別の手を試しても状況は変わらない。

**`ignored` が空でなければユーザーに知らせる。** 発言として数えなかったファイルの一覧で、
本来の発言がそこに落ちていると、会話の記録が欠けたまま進むことになる。

**`unknown_keys` も同じ扱い。** `room.json` にあるが状態として読まないキーの一覧。
ユーザーが書き換えたつもりの値がここにあると、書き換えが効いていないまま会話が進む。
`max_hops` を増やしてもらった直後は特に確かめる。

## コマンド

```
cxtalk open    --topic <text> [--max-hops 5] [--as <name>]
cxtalk join    <room_id> [--as <name>]
cxtalk say     <room_id> --text-file <path> --advanced <true|false> [--as <name>]
cxtalk receive <room_id> [--timeout 100] [--as <name>]
cxtalk status  <room_id> [--as <name>]
cxtalk close   <room_id> [--reason manual] [--as <name>]
cxtalk ls      [--open <true|false>]
```

`--timeout` は既定のまま使う。100 秒は呼び出し側の制限に収まる値として選ばれており、
延ばすと外側から打ち切られて原因の分かりにくい失敗になる。
長考にはリトライで対応する。

`--reason` は `hop_limit` / `stale` / `no_response` / `idle` / `manual` のいずれか。
自由文は受け付けない。閉じた理由は報告に載るため、決まった語で残す。
自分から打ち切るときは `manual`。省略しても `manual` になる。

`check` というサブコマンドもあるが、これは Stop hook が使う。自分から呼ぶ必要はない。

### 名乗り

`--as` を省略すると作業ディレクトリの名前を名乗る。通常はこれでよい。

同じディレクトリで複数のセッションを動かす場合だけ、`--as` で名乗り分ける。
**その場合は `--as` を受けるコマンドすべてに付ける。** 一つでも省くと作業ディレクトリの
名前に戻り、同名の参加者が既にいれば再入場として扱われて、参加者が 1 人のままになる。
**待っても相手が来ないなら名乗りを見直す。**

名前は 1 文字以上 64 文字以内。数字だけの名前、`.` と `..`、制御文字、
ファイル名に使えない文字（`\ / : * ? " < > |`）は使えない。

### 参加者は 2 人まで

3 人目の `join` は `room_full` で断られる。発言権は 2 者の交代として回るため、
3 人目には順番が来ない。

会話の読み書きは参加者に限られる。`receive` と `say` と `close` は、
参加していない名前で呼ぶと `not_a_participant` で断られる。
`status` と `ls` は状態の確認なので誰でも呼べるが、発言の本文は返らない。
読みたければ `join` する。

## 開く

```
cxtalk open --topic "<論点>"
```

`--topic` には**詰める範囲を具体的に書く**。
「認証まわり全般」ではなく「トークンの有効期限をどちらが持つか」。
範囲が曖昧なままだと往復が発散し、上限に達しても何も決まらない。

**進め方の条件があれば、それも `topic` に書く。** 「主張には実測を添える」のような約束は、
参加した側が `join` の時点で調べ始めるため、第一声では間に合わない。
`topic` は参加した側が最初に受け取る唯一の文になる。

**依頼したら、そのまま `receive` を呼ぶ。** 相手が参加した時点で返るので、
参加できたかをユーザーに確認する必要はない。
**参加を待てるのは 15 分まで**なので、依頼にそれを添える。

`hook_last_run` の `null` は「まだ記録が無い」であって「壊れている」ではない。
会話を始めるまで hook は実装を起動しないため、最初の `open` では必ず空になる。

**往復の上限は扱う件数に合わせる。** 既定は 5。一つの判断を詰めるだけなら `--max-hops 3`、
複数の案件をまとめて扱うなら件数と同じだけ見込む。開いた後に変えるコマンドは無く、
足りなくなったらユーザーが `room.json` の `max_hops` を書き換える。
**頼むときは値の形も添える**（1 以上 4999 以下の整数）。形を外すとそのルームは読めなくなる。
**書き換えられたら、理由を `say` で 1 件書く。** 相手からは条件が変わった事実しか読めない。

## 参加する

```
cxtalk join <room_id>
```

`messages` に未読が入って返る。読んでから `next` に従う。

ターンが切れたあと再び会話に戻るときも同じコマンドを使う。
二度目以降は前回の続きだけが返る。

## 発言する

```
cxtalk say <room_id> --text-file <path> --advanced <true|false>
```

**本文はファイルに書いてから渡す。**`--text` で直接渡すと引数をシェルが解釈するため、
本文のバッククォートが実行され、**別の文字列が黙って相手に届く。**
短い一行なら `--text '<本文>'` でもよいが、単一引用符の中にアポストロフィは書けない。

本文に載せること。

- 自分の側の制約と、そう考える理由
- 相手に判断してほしい点
- 選択肢があるなら、それぞれの利害

載せないこと。

- 相手が自分のコードを読めば分かること
- 長い引用。要点を自分の言葉で書く
- 決定の宣言。決めるのは人間

### --advanced の判断

`--advanced` は「この発言で論点が前に進んだか」の自己申告。楽観的に倒れやすいので、
**迷ったら `false`。** 同意・お礼・要約・確認だけの発言、言い換えただけの発言は `false`。

`false` が 2 回続くとルームは閉じる。**会話の締めくくりは `false` でよく、
閉じられないように `true` と申告しない。** 受け取った `messages` の `advanced` で、
自分の返し方は変えない。

## 待つ

```
cxtalk receive <room_id>
```

相手が発言するまで戻らない。時間切れも待機の上限も `hint` が次の手を告げる。

**応答がないからといって、勝手に会話を打ち切ったり自分だけで結論を出したりしない。**
相手が停止したのかまだ書いているのかは区別できない。決めるのはユーザー。

**待ち続けてもルームは 30 分の無音で閉じる。** 呼び直しでは止まらない。
そのとき相手が書いていた本文は `closed-` で始まるファイルに残るので、
`ignored` に出ていたらユーザーに場所を伝える。

状態だけを見るなら `cxtalk status <room_id>`、一覧は `cxtalk ls`。
**どちらも既読にならず、状態も変えない。** 続きのある会話を探すだけなら `ls --open true`。
どのルームの話かをユーザーに尋ねるときは `closed_reason` と `hops_used` を添える。
話し切った会話と、一言も交わさずに閉じたルームは `status` だけでは区別が付かない。

**`unreadable` に room_id が並んでいたら、そのルームは状態を読み取れない。**
壊れていることが伝わる窓はここだけなので、並んでいたらユーザーに知らせる。
何が読めないかは、その room_id で `status` を呼べば hint が名指しする。
原文は `log_root` の下の、room_id と同じ名前のディレクトリにある。

## 閉じて報告する

`next: report` が返ったら会話は終わり。報告に何を載せるかは `hint` が挙げる。

**要約には必ず `log_path` を添える。** 要約を書くのは自分であり、
自分に都合よく丸めていないことを人間が検証できる必要がある。
全発言は 1 発言 1 ファイルとしてそこに残っている。

`messages` が付いていたら**要約する前に必ず読む。** 会話が終わる経路は複数あり、
どれも相手の最後の発言が未読のまま残ることがある。

途中で打ち切るときは `cxtalk close <room_id>`。相手側にも終了が伝わる。

## 守ること

**ルームは論点を詰める場であり、決定する場ではない。**
成果物は「決定」ではなく「整理された論点と、双方の見解」とする。

会話で出た結論をそのまま実装しない。ユーザーに報告し、承認を得てから動く。

**会話の途中でターンを終えない。** `say` を送ったら、同じターンの中で `receive` を呼び続ける。
途中経過をユーザーに報告するのは構わないが、報告して**ターンを終えてはいけない。**
止まったセッションは、未読が届いても Stop hook では起こせない。hook はターンの終わりに走るので、
ターンが来ないセッションには永久に走らない。次に動けるのは人間が話しかけたときだけになり、
その間にルームは 30 分の無音で閉じる。**相手は人間に伝言を頼むしかなくなる。**

人間が席を外している間も往復は進む。そのことを踏まえて振る舞う。

- 相手の発言を鵜呑みにしない。自分の側の制約と食い違うなら、その場で指摘する
- 埋まらない対立を往復で押し切ろうとしない。
  二人の間で決められないと分かった時点で、対立したまま持ち帰る
- 最後の発言で押し切らない。上限に達する発言に相手は反論できない。
  say を促す hint が「これが最後の発言になります」と告げたら、
  合意していない点を合意したことにせず、対立を対立のまま書く
- 相手に何かを約束しない。実装するかどうかはユーザーが決める
