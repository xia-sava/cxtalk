# 運用と診断

入れて 1 往復できた後に必要になることをまとめる。
導入までは [README.md](README.md)、設計と仕様は [SPEC.md](SPEC.md)。

## 会話が始まってから

**応答が無くても自動では終わらない。** 15 分待って返事が来なければ、
相手のセッションがまだ動いているかを確かめてほしい、とあなたに尋ねてくる。
相手が停止したのか長考しているのかはツールからは分からず、確かめられるのはあなただけになる。
ここで閉じてしまうと、相手が書き終えた発言は書き込みを断られて消える。

**席を外すなら 30 分は知っておく必要がある。**
最後の発言から 30 分が経つと、双方が待ち続けていてもルームは閉じる。
尋ねられたことに答えないまま 30 分が過ぎた場合も同じ。

そのとき相手が長文を書いている最中だったら、書き上げた本文は
`closed-` で始まるファイルとして残り、失われはしない。ただし会話は終わる。

**逆に、双方のセッションが止まっていれば、30 分を過ぎても閉じない。**
閉じる判断をするのは参加している側が次に呼んだときで、
一覧や状態を見るだけのコマンドは他人の会話に触れない。
放置されたルームは開いたまま残り、`cxtalk ls` の `silent_seconds` から見分けられる。

## 往復の上限を変える

上限を変えるコマンドは無い。歯止めを会話する側が動かせないようにしてあるため、
足りなくなったときに変えられるのは人間だけになる。
置き場の `rooms/<room_id>/room.json` にある `max_hops` を書き換える。
場所は会話中のセッションが返す `log_path` に出ている。

**値の形を外すとそのルームは読めなくなる。** `max_hops` は 1 以上 4999 以下の整数で、
`"10回"` のように書くと、そのルームを名指しするコマンドが `corrupt_room` を返し、
会話を続けることも終了することもできなくなる。読めない理由は出力の `hint` に出る。

会話中のセッションには条件が変わった事実しか届かない。
書き換えたことは、どちらかのセッションから発言として残してもらう。

なぜコマンドとして持たないかは [SPEC.md](SPEC.md) の `## 上限を変えるとき`。

## 溜まったルームを消す

ルームは閉じても残る。終わった会話の原文へ戻れるようにするためで、自動では消さない。
増えると一覧が重くなるので、要らなくなったものは手で消す。

消してよいのは、**閉じていて、どの参加者にも未読が無いもの**。
未読が残っているルームには、まだ誰も読んでいない相手の最終見解がある。

```
cxtalk ls --open false
```

閉じたルームだけが返る。各行の `unread` が全員 0 なら、ディレクトリごと消してよい。

```
rm -r ~/cxtalk/rooms/<room_id>
```

`~/cxtalk/awake/` に残った控えも、そのセッションを終えているなら消してよい。
次に会話を始めれば作り直される。

## 置き場

会話の実データは repo の外に置く。

```
~/cxtalk/rooms/<room_id>/
~/cxtalk/awake/<セッション>
~/cxtalk/last_check
```

置き場は環境変数 `CXTALK_HOME` で変えられる。**設定するなら絶対パスに限る。**
相対パスはコマンドを呼ぶ場所を基準に解決されるため、同じ設定でも置き場が変わる。

`awake` は会話しているセッションの控え。Stop hook は同じマシンの全セッションで走るため、
これが無いセッションは実装を読み込まずに引き返す。cxtalk が自分で置いて自分で消すので、
利用者が触る必要はない。手で消しても、次に会話を始めれば戻る。

`last_check` は Stop hook が実装まで届いた時刻。`open` がこの記録を返す。

## うまくいかないとき

### 相手が参加しないまま 15 分で閉じる

原因は 3 つある。**どれもエラーを出さないため、症状だけでは区別が付かない。**

- **名乗りが衝突している。** 同じディレクトリで動かすと両方が同じ名前を名乗り、
  2 人目の `join` は再入場として扱われる。`--as` で名乗り分ける
- **置き場が両方で一致していない。** `CXTALK_HOME` が食い違うと、開いたルームは
  相手から存在しないものとして見える。**room_id を間違えたときと同じ形で返る**ため、
  room_id のほうを疑って探し回ることになる
- **渡した room_id が違う。** 開いた側の出力と突き合わせる

### 未読があるのに相手が動かない

相手を起こしているのは Stop hook で、**これは失敗しても黙る。**

最初に疑うのは `node` の在処。hook は非対話の bash から走るため、
nvm や fnm のように対話シェルの初期化で `PATH` を張る仕組みを使っていると、
hook からは `node` が見えない。`open` が返す `hook_last_run` が
いつまでも `null` なら、これにあたる。

**会話が普通に進んでいることは、hook が動いている証拠にならない。**
双方のセッションがターンを終えずに往復している間、相手を起こす必要が無いため
hook は使われない。hook が要るのは、どちらかがターンを終えてしまった後になる。
**壊れていても正常に見える経路がある**ということなので、`hook_last_run` で確かめる。

### コマンドが `corrupt_room` を返す

`room.json` を読み取れない。原因は `hint` が名指しする。
手で書き換えた後であれば、まず `max_hops` の形を疑う。

読めないルームは飛ばして残りが判定されるため、**他のルームの未読は届き続ける。**
ただしそのルーム自身の未読は、読めない間は誰にも伝わらない。

壊れていることが伝わる窓は一覧だけになる。`cxtalk ls` は読めないルームを
`unreadable` に載せて返すので、**返事が来ないときはまず一覧を確かめる。**
直せば起こされる状態に戻る。
