# cxtalk 仕様（暫定）

起案: 2026-08-07

## 解決する問題

同一マシンで複数の Claude Code セッションを並行して動かすとき（例: `backend` と `frontend`）、
セッション間の情報の受け渡しを人間が仲介している。往復が増えるほど人間の手間が線形に増える。

既存手段の限界:

| 手段 | 限界 |
|---|---|
| 各 repo に md の送信箱を持ち append する | 相手が読みに行くタイミングを人間が作る必要がある。ファイルが肥大し、未読管理がない |
| メッセージを送るツールを用意する | 受信側のセッションは入力待ちで停止しているため、送っても届かない |

## 基本方針

停止中のセッションを外から起こすことはできない。そこで**両者が自発的に会話モードに入る**構成にする。

`receive` をロングポーリングにすれば、コマンド実行の内側で相手の発言を待てる。
コマンドはプロセスが終了するまで呼び出し側が待つため、
「相手が発言するまで終了しないコマンド」として実装できる。

```
BE                     cxtalk                     FE
 |  open                 |                         |
 |<-- "r-a3f9" ----------|                         |
 |                       |<-- join r-a3f9 ---------|
 |  say "..."            |                         |
 |---------------------->|------------------------>|  receive が返る
 |  receive …待機        |                         |
 |                       |<-- say "..." -----------|
 |  receive が返る <------|                         |  receive …待機
```

人間の操作は最初の `open` / `join` の 2 回だけで、以降の往復は自動で進む。

## 構成

状態は共有ディレクトリ上のファイルに置く。プロセス間で状態を共有するため、
常駐サーバを持たずコマンド実行のたびにファイルを読み書きする。

```
~/cxtalk/rooms/r-a3f9/
  room.json
  messages/
    0001-backend.md
    0002-frontend.md
    0003-backend.md
```

メッセージのファイル名に送信者を含めるため、万一連番が競合しても上書き事故にはならない。

**ファイル名は 4 桁の通し番号・送信者・`.md` からなる形に限る。** 発言権も往復数も
通し番号から導くため、ここには room.json より重い状態がある。`log_path` は原文を読ませる
場所として人に案内しているので、関係のないファイルが増えることを前提に扱う。

| ファイル | 扱い |
|---|---|
| 形に合わない | 発言ではない。数えない |
| 形は合うが通し番号が詰まっていない | 断る |
| 形は合うがヘッダが無い | 断る |

ヘッダは日時と申告を持つ。

```
---
at: 2026-08-07T14:32:10+09:00
advanced: true
---
本文
```

**申告をここに残すのは、`stale_streak` から復元できないためである。** 連長は両者で共有する
整数 1 つで、直近の連続数しか覚えていない。前に進む発言が続かなかったことを理由に閉じても、
どの発言がそう申告したのかを後から誰も確かめられない。

**本文と区切る位置は、最初に現れる `\n---\n` とする。** ヘッダが `at:` で始まると
決まっているため、本文が同じ行を含んでいても、ヘッダの終わりが必ず先に来る。
最後の一つを探す形や貪欲な照合に変えると、本文に区切り行を書いた発言が黙って切れる。

**申告が書かれていない発言は、`false` に寄せず「分からない」として返す。**
この形式より前に書かれた発言が実在し、寄せるとそう申告していない発言に申告が付く。

**数えなかったファイルは出力に並べる。** 黙って落とすと、本来の発言が誤って落ちたときに
発言が消えたようにしか見えない。`ls` の `unreadable` と同じ理由による。

断るときは開くべきファイルを名指しする。名指ししないと、人は room.json を開いて何も見つけられない。

状態の置き場は環境変数 `CXTALK_HOME` で変えられる。未設定なら `~/cxtalk` とする。
**絶対パスに限る。** 相対パスは作業ディレクトリを基準に解決されるため、
同じ設定でも呼ぶ場所で置き場が変わり、開いたルームが見つからなくなる。

## 状態モデル

```json
{
  "id": "r-a3f9",
  "topic": "確認テストの合格基準は正答数か正答率か",
  "status": "open",
  "opener": "backend",
  "max_hops": 5,
  "stale_streak": 0,
  "last_activity_at": "2026-08-07T14:32:10+09:00",
  "participants": {
    "backend": { "last_read": 3, "timeouts": 0, "join_timeouts": 0 },
    "frontend": { "last_read": 2, "timeouts": 1, "join_timeouts": 0 }
  },
  "closed_reason": null
}
```

各フィールドが担う振る舞い:

| 振る舞い | フィールド | 判定 |
|---|---|---|
| 会話が終わらない問題 | `max_hops` | 発言の通し番号から往復を数え、上限で close |
| 中身のない応酬の検知 | `stale_streak` | `advanced: false` で +1、`true` で 0 に戻す。2 で close |
| 双方が待ち合うデッドロック | `opener` | 発言権を通し番号から導く。自分の番なら `receive` は即座に返る |
| 応答が来ない | `participants[].timeouts` | `receive` タイムアウトで +1、相手の発言が届くと 0 に戻す。上限で人間に返す |
| 相手が参加しない | `participants[].join_timeouts` | 参加を待つ時間切れで +1。上限で close |
| 放置されたルームの掃除 | `last_activity_at` | 30 分無音で close |
| 再入場時の差分取得 | `participants[].last_read` | これ以降のメッセージを返す |

### 書き換えられた値は読み込みで確かめる

上限を変える手段として人間が `room.json` を書き換えることを案内している以上、
読み込んだ値が使える形かをそこで確かめる。

| 値の状態 | 扱い |
|---|---|
| 欠けていて、安全な既定がある | その既定へ寄せる |
| 欠けていて、安全な既定が無い | 読み取れないものとして断る |
| 書かれているが解釈できない | 読み取れないものとして断る |
| 同じ意味の値に複数の表現がある | 一つの表現へ正規化する |
| 同じ値が文脈によって別の意味になる | 読み取れないものとして断る |

寄せてよいのは、結果が「まだ読んでいない」「まだ数えていない」に倒れるものだけで、
`last_read` `timeouts` `join_timeouts` `stale_streak` がこれに当たる。
断るのは `id` `status` `closed_reason` `max_hops` `last_activity_at` `opener` `participants`。

**正規化は寄せることと別に扱う。** 寄せるかどうかは安全側に倒れるかで決まるが、
正規化は同値なものから代表を選ぶだけで、安全側という概念を持たない。
混ぜると、寄せる条件を次に判断する者が確かめられなくなる。

**5 番目に当たるのは、解釈が呼び出しごとに変わる値である。** `CXTALK_HOME` の相対パスが
これで、作業ディレクトリを基準に解決されるため、同じ設定でも呼ぶ場所で置き場が変わる。
寄せ先を決めることはできるが、決めた基準が呼ぶ側の期待と食い違ったときに気づけない。

`max_hops` が読めなければ上限が来ず、`last_activity_at` が読めなければ掃除が効かない。
`opener` が参加者から外れていれば双方の番が来ず、
`closed_reason` は定義していない文字列が報告にそのまま乗る。
どれもこちらで決めると、歯止めや発言権が黙って変わったまま、
筋の通った説明が人間に届く。

**寄せ先があることと、寄せて機能することは別である。** `last_activity_at` を
現在時刻へ寄せると、書き戻す経路を持つのは会話するコマンドだけなので、
誰も使っていないルームでは読むたびに無音の長さが 0 に戻り、掃除が永久に効かない。
掃除が対象とするのは、まさにその書き戻されないルームである。

**確かめるのは 2 番目と 3 番目に当たるものだけとする。** 後から増えるフィールドは
基本的に 1 番目に当たる。ここを決めておかないと、フィールドを 1 つ増やすたびに、
それを持たない既存の状態ファイルが一斉に読めなくなる。

### 定義していないキーは断らず、名前を挙げて人間に渡す

上の表はいずれも「あるべきキーの値」を扱っており、「あるべきでないキーの存在」には
当たらない。定義を外したフィールドは実際に古い `room.json` へ残り続ける。

これを断らないのは、**キー名と意味を 1 対 1 に保つ**ことを規則とするためである。
意味を変えるときはキー名も変える。この規則が守られている限り、
定義していないキーは常に古い意味のままであり、無視して害が無い。

ただし黙って無視はしない。上限を変える手段として人間が `room.json` を書き換える以上、
書き換えた値が読まれていないことは伝わらなければならない。
`join` と `status` が `unknown_keys` として名前を挙げ、hint で人間に知らせる。

**発言のファイルの `ignored` と窓を分ける。** 同じ「数えなかったものを名前で挙げて
人間に判断させる」形だが、直しに行く先が `room.json` と `messages/` で違う。
一つにまとめると、受け取った側は発言が落ちたものとして読む。

### 発言権と往復は通し番号から導く

発言権も往復の回数も状態として持たず、メッセージの通し番号 `seq` から求める。

発言権は先手が偶数番、後手が奇数番を受け持つ。`seq` が 0 のとき先手に発言権がある。
先手はルームを開いた者で、`opener` として記録する。この値は会話の間ずっと変わらない。

往復は両者が 1 回ずつ発言して 1 とし、`Math.ceil(seq / 2)` で求める。
`hops_left` は `max_hops` との差。`max_hops` の既定値 5 は、
各参加者が 5 回まで発言できることを意味する。

**`max_hops` の上限はファイル名の桁から決まる。** 通し番号は 4 桁で綴るため、
`seq` が 9999 を超えると桁が増え、並び順も番号も送信者の切り出しも変わる。
1 往復で 2 件書くので上限は 4999 とし、開くときと `room.json` を読むときの
両方で同じ範囲を受ける。人が書き換える唯一の手段が後者なので、
片方だけに置くとそこから素通りする。

導出にする理由は、発言の記録と発言権の受け渡しを 1 つの書き込みにまとめるため。
両方を状態として持つと、メッセージを書いてから状態を書くまでの間に落ちたとき、
発言が存在するのに発言権が渡っていないルームが残り、誰も発言できなくなる。
`seq` はファイル名から数えられるので、メッセージを書いた時点で発言権も確定する。

発言権が 1 人ずつ順に回る限り、両者が同時に `say` することはない。
連番の採番でも競合しないため、ファイルロックやトランザクションを要しない。

**排他が及ぶのは発言だけである。** `close` と `receive` の待機回数の書き込みは
発言権の外にあり、どちらも相手の番の最中に走る。状態ファイルは全体を書き直すため、
読んでから書くまでの間に相手が書いた内容は、こちらの持つ写しで上書きされる。

この窓は塞がない。ロックを持たない設計を選んだ以上、読みと書きの間は必ず開く。

**写しを書き戻す以上、その間に相手が書いた内容はすべて消える。**
閉じた理由が別の理由に変わることも、閉じられたルームが `open` に戻ることも、
参加したばかりの相手が参加者から消えることも、同じことの現れ方である。
数え上げると、状態が増えるたびに数え直すことになる。

気づきやすさは現れ方によって違う。理由が変わるものは報告の文面に出るが、
`open` に戻るものと参加が消えるものは、どちらの側の出力にも出ない。
参加が消えた場合、加わった側は `ok: true` を受け取って待機に入り、
待っていた側は上限に達したあとで room_id が伝わったかを確かめさせる。
room_id は正しく伝わっており、消したのは待っていた側である。

### 参加者は 2 人まで

`join` は 3 人目を断る。発言権を 2 者の交代として導いており、
3 人目には発言権が回らないまま参加だけができてしまうため。

会話の読み書きは参加者に限る。`receive` と `say` と `close` は
参加者でない名前からの呼び出しを断る。`status` と `ls` は状態の確認であり
本文を返さないので、誰が呼んでもよい。読みたければ `join` する、という一本の規則にする。

**参加者を名前で引く手段は 1 つに限る。** `in` と添字はプロトタイプチェーンにも答えるため、
`toString` のような名前が参加者として通り、`close` が成立し、`receive` が
上限に当たらない待機に入る。引き方が各所に散ると、直す先を数え上げた本人がその場で 1 つ落とす。
名前の側を禁止語で狭める形は採らない。**断るべき字面の集合を決めているのが repo の外**であり、
入口で断る形は、断る条件を自分で決められるときにしか使えない。

### 死活監視は行わない

相手が長考中の場合、相手はコマンドを実行しないため最終アクセス時刻が更新されない。
これは相手のセッションが停止している場合と区別がつかない。長考は数分に及ぶことがあり、
閾値を短くすれば正常な相手を切断し、長くすれば停止した相手を待ち続ける。

したがって「相手が生きているか」は判定せず、「自分がいつまで待つか」だけを決める。

- 正常終了は明示的な `close`
- 会話中に待ちきれなくなったら、閉じずに人間へ判断を返す
- 参加が成立しないまま待ちきれなくなったら `receive` のリトライ上限で close
- 放置されたルームは `last_activity_at` によるアイドル掃除

**相手が生きているかを確かめられるのは人間だけである。** 相手のセッションの画面は
ディスクに現れないが、人間はそれを見られる。判定できないものを時間で打ち切る代わりに、
判定できる者へ渡す。

## 参加者名

1. `--as` の指定があればそれ
2. 無ければ cwd の repo 名
3. **同名の参加者が既にいれば再入場として扱う**

別人として参加したい場合のみ、明示的に別名を名乗る。
再入場はターンが切れるたびに発生する頻繁な操作であり、
同一 repo で複数セッションを動かすのは稀であるため、頻繁な側を既定の動作にする。

在室状態は持たない。一度 `join` すれば close まで参加者であり、`join` は冪等。

名前はメッセージのファイル名に埋まり、発言権の判定にも使う。次を満たさない名前は断る。

- 空でなく 64 文字以内であること
- 数字だけでないこと。状態を読み書きすると並び順が変わり、先手の判定が入れ替わる
- `.` および `..` でないこと
- パス区切りとファイル名に使えない文字を含まないこと
- 制御文字を含まないこと

**名前は受け取った時点で一つの表現へ正規化する。** 同じ字面でも合成済みと分解済みで
別の文字列になるため、寄せないと見た目の同じ名前が別人として扱われ、
断る文と参加者一覧に同じ字面が並ぶ。

**ファイル名から取り出した名前も、比べる前に同じ表現へ寄せる。** 名前はファイル名を
経由して戻ってくる。保存した綴りをそのまま返さないファイルシステムがあり、
そこでは自分の発言が自分のものと一致しなくなる。保存し直す必要はない。

## コマンド

```
cxtalk open   --topic <text> [--max-hops 5] [--as <name>]
cxtalk join   <room_id> [--as <name>]
cxtalk say    <room_id> --text <text> --advanced <true|false> [--as <name>]
cxtalk receive <room_id> [--timeout 100] [--as <name>]
cxtalk status <room_id> [--as <name>]
cxtalk close  <room_id> [--reason manual] [--as <name>]
cxtalk ls
cxtalk check  [--as <name>] [--room <room_id>]
```

`check` 以外は JSON を stdout に出力する。

### 共通規約

すべてのサブコマンドが `ok` / `next` / `hint` を返す。
異常も終了コードで表さず、正常な JSON として返す。
実行失敗として返すと、受け取った側が独自の回復動作を始めることがあるため、
必ず次の行動を添えて返す。実装の誤りであっても同じで、
そのときこそ独自の回復動作をされると困る。見せる先は終了コードではなく `hint` の文面とする。

**この約束は入口で保証する。** 標準出力に JSON を 1 行返して 0 で終わることは、
個々の操作の性質ではなく呼び出し側との約束である。操作ごとに囲んで回ると、
次に足した操作が同じ穴を開ける。引数の解釈を含めて入口の内側に置き、
どの経路から投げても約束の側を保つ。

既に返した後で投げた場合は 2 行目を足さない。呼び出し側は 1 行として読むため、
足すと読み取りが壊れる。握り潰さずに済ませるため、標準エラーへ 1 行だけ残す。

書き込めないことは、値が読めないことと別の名前で返す。
確かめてほしいものが中身ではなく置き場所と権限であり、人の取る行動が違う。

`next` は 5 値のみ。

| next | 意味 |
|---|---|
| `say` | 発言する番 |
| `receive` | 相手を待つ |
| `report` | 会話終了。要約して人間に報告する |
| `ask_user` | 人間の入力が必要 |
| `retry` | 呼び出しに誤りがある。直して呼び直す |

`retry` は呼び出した側が自力で直せる誤りに使う。値の欠落や解釈できない値がこれにあたる。
人間を経由せずに直せるものを `ask_user` へ回すと、人間が席を外している間に会話が止まる。
逆に `no_such_room` のように room_id を人間から受け取り直す必要があるものは `ask_user` を使う。

状態ファイルを読み取れない場合も JSON で返す。`corrupt_room` として `ask_user` に回し、
確認すべき場所と読み取れない理由を添える。中身を直せるのは人間だけであり、
呼び出した側が直せる誤りではない。
実行の失敗として返すと、受け取った側が独自の回復動作を始める。

理由を添えるのは、構文として読めない場合と値が使えない場合とで、
人間が探す場所が変わるため。前者の原因は解析器が返すので固定の文に畳み、
後者は確かめた側が何の値かを書く。

`next: "report"` を返す出力と `status` / `ls` は、会話の原文が置かれたディレクトリを
`log_path` として返す。報告する側が要約を書くため、要約を経ずに人間が原文へ辿り着ける
導線がないと、丸められた内容を検証できない。

### 引数の解釈

フラグは必ず値を取る。値を取らないフラグを持たない。
そのため次の語がハイフンで始まっていても値として扱う。
`--text` の本文は箇条書きや水平線で始まることがあり、
ハイフンを値の終わりと見なすと本文が失われる。

ただし値がフラグ名そのものだった場合は誤りとして断る。
値を書き忘れると次のフラグを値として飲み込み、
残りが揃っていれば成功したまま誤った本文が相手に届く。
弾くのはフラグ名と一致する語だけに限り、`---` や `- ` で始まる本文は通す。

コマンドが受け付けないフラグを渡された場合も断る。
`--max-hops` を `--max-hop` と綴り違えると、指定したはずの上限が既定値のまま進み、
気づく手段がない。値の書き間違いと同じく `retry` で返す。

room_id の欠落も `retry` で返す。存在しないルームとして扱うと、
呼び出した側が自力で直せる誤りを人間へ回すことになる。
コマンド名を解釈できない場合も同じく `retry` とし、使えるコマンドを並べる。

数値を取るフラグは 1 以上の整数だけを受け、それ以外は `ok: false` で断る。
NaN はファイルを経由すると null になり、比較の結果が静かに変わる。
入力の時点で弾かないと、原因から離れた場所で誤った振る舞いが現れる。

`hint` は日本語で、具体的な行動を書く。受け取る側への指示として機能する。

---

### open

```json
{
  "ok": true,
  "room_id": "r-a3f9",
  "as": "backend",
  "topic": "確認テストの合格基準は正答数か正答率か",
  "max_hops": 5,
  "turn": "backend",
  "next": "ask_user",
  "hint": "ルームを開きました。ユーザーに『相手のセッションで cxtalk join r-a3f9 を実行するよう伝えてください』と依頼し、そのまま receive を呼んでください。相手が参加した時点で receive が返ります。"
}
```

`--topic` は必須。省略を許すと論点のないルームができ、
block 時に人間の画面へ出る 1 行からも、何の会話が進んでいるのかが読めなくなる。

この時点で相手は room_id を知らないため、`next` は `say` ではなく `ask_user`。
room_id を相手へ渡すには人間を経由するしかない。

渡した後は `receive` で相手の参加を待てる。参加が成立したかを人間に伝えてもらう必要はない。
人間の操作を room_id の受け渡し 1 回だけに減らす。

---

### join

| 状況 | ok | status | next |
|---|---|---|---|
| 新規参加 | true | open | `receive` |
| 再入場・自分の番 | true | open | `say` |
| 再入場・相手の番 | true | open | `receive` |
| 閉じたルーム | true | closed | `report` |
| 存在しない room_id | false | — | `ask_user` |

```json
{
  "ok": true,
  "room_id": "r-a3f9",
  "as": "frontend",
  "rejoined": true,
  "topic": "確認テストの合格基準は正答数か正答率か",
  "status": "open",
  "hops_left": 3,
  "messages": [
    { "seq": 3, "from": "backend", "at": "2026-08-07T14:32:10+09:00",
      "text": "..." }
  ],
  "turn": "frontend",
  "next": "say",
  "hint": "未読 1 件。再入場です。"
}
```

`messages` は再入場なら `last_read` 以降の差分、新規参加なら全件。
閉じたルームへの join も `ok: true` で未読を返す。
相手の最後の発言を読まないまま要約させないため。全文は `log_path` にある。

### 報告へ移る経路では未読を渡す

`next: "report"` を返す経路は 8 つある。

| 経路 | 未読の扱い |
|---|---|
| `join` / `receive`（閉じている） / `close` | `messages` に載せて既読にする |
| `receive`（待つ間に閉じられていた） | 同上。閉じた理由も状態から読む |
| `say`（閉じたルームへの発言） | 同上 |
| `say`（自分の発言で上限に達した） | 発言の前に控えた未読を載せる |
| `status`（参加している名前） | 返さない。hint で件数と読む手段を伝える |
| `receive`（参加待ちの `no_response`） | 相手が来ていないため発言が無い |

一部の経路だけが未読を渡す形にすると、受け取らなかった側は未読が無いものとして扱い、
相手の最終見解を読まないまま要約する。読み落としたことは出力からは分からないため、
誤った説明が付いたまま人間に届く。

`say` が自分の発言で閉じる場合は順序に注意がいる。`last_read` を自分の発言番号へ進めるため、
発言を書く前に未読を控えないと、読んでいない相手の発言まで既読になる。
この場合は未読を持ったまま報告へ進むのではなく、未読を消してから報告へ進むことになり、
あとから `receive` を呼んでも返らず、読み落としに気づく手段が原文しか残らない。

### 未読に自分の発言を含めない

発言を書いてから状態を書くまでの間に落ちると、自分の発言が未読として残る。
そのまま渡せば、自分が書いたものを相手の最終見解として読ませ、それを要約させることになる。

読む側も数える側も同じ条件を通す。同じ条件が各所に散ると、片方だけが直る。

**見せるものと既読の位置は別の量として扱う。** 自分の発言しか残っていない場合、
返すものは無いが位置は進める。進めないと、`receive` を呼んでも返らないのに
件数だけが残り続け、消えない未読になる。

**既読の位置は、返した内容より古い側へ倒す。** 位置と内容はそれぞれ別の読み取りから得るため、
その間に相手が発言することがある。位置を後から読むと、返していない発言を既読にしてしまい、
以後どのコマンドでも読めなくなる。位置を先に読めば、同じ発言が二度返ることはあっても失われない。
再現には読み取りの間に遅延を挟む必要があり、外から見た振る舞いの検証では固定できない。
未読を返す経路を書くときは、この順序を守る。

発言権を `seq` から導く以上、相手の発言を受け取らないまま `say` を呼ぶ経路は成立する。
`status` が既読にしないこととも噛み合うため、読まずに発言して閉じることは起こりうる。

`status` だけは `messages` を返さない。確認しただけで既読にしないことがこのコマンドの役割であり、
既読にすると取りこぼしを生む。代わりに hint で未読の件数と `receive` で読むことを伝える。

**発言が 1 件も無いルームでは、どの経路でも要約を求めない。** 起きていない会話に
要約を求めず、room_id が伝わったかを確かめさせる。閉じ方は `no_response` に限らない。
掃除が先に当たれば `idle` で閉じ、そのあとに相手が `join` してくることがある。

---

### say

`--advanced` は必須。省略可にすると省略されるため。

| 状況 | ok | next |
|---|---|---|
| 通常 | true | `receive` |
| hop 上限に到達 | true | `report` |
| stale 2 連続 | true | `report` |
| 自分の番でない | false | `receive` |
| 閉じている | false | `report` |
| 参加者が自分だけ | false | `receive` |
| 参加していない | false | `ask_user` |

複数の条件が同時に成立するときは、参加しているかを先に見る。
参加していない相手に閉じている旨を返すと、会話の要約と原文の場所まで渡すことになる。

参加者が自分だけの場合は `receive` を返す。相手を待てば解決するため、
人間を呼ぶと席を外している間だけ会話が止まる。

**参加者が 1 人だと分かる場所ではすべて同じ見立てを添える。** `join` `status` `say` と、
参加待ちの時間切れが同じ状況を説明する。名乗りの取り違えは 900 秒待たなくても
その場で判定できるので、待たせてから告げると予算を使い切った後にしか届かない。
「相手が来なかった」と「相手は来たが同じ名前で来た」は記録上まったく同じ形になるため、
どちらであるかを確かめられるのは人間だけである。

参加していない名前には `join` を勧めない。room_id を取り違えていた場合、
無関係のルームへ 2 人目として入り、本来の相手を `room_full` で締め出すことになる。
名乗りと room_id のどちらを取り違えたかは会話する側では決められないため、人間に確かめさせる。

参加者が自分しかいないルームでは発言を受け付けない。
`turn` は自分以外の参加者へ渡すため、相手がいなければ自分に戻り続ける。
そのまま通すと、独りで上限まで消費しながら相手が沈黙しているようにしか見えない。

上限に達する発言になる場合は、say を促す時点でそう伝える。
`receive` / `join` / `status` が返す hint に含める。
書いた後で知らせても、相手が応答できないことを踏まえて書き直すことはできない。

通常:

```json
{
  "ok": true, "seq": 5, "hops_left": 2,
  "turn": "backend", "next": "receive",
  "hint": "発言を記録しました。相手の応答を receive で待ってください。"
}
```

終端:

```json
{
  "ok": true, "seq": 6, "hops_left": 0,
  "status": "closed", "closed_reason": "hop_limit", "next": "report",
  "hint": "往復上限に達したのでルームを閉じました。合意できた点・未解決の点・持ち帰る宿題を要約してユーザーに報告してください。ここで出た結論は提案であり、実装はユーザーの承認を経てください。"
}
```

上限に達した発言は、相手が応答する枠のないまま記録される。
この発言に相手は反論できないことを hint に含める。
書いた側が最後の見解を押し通す形になるため、それを承知で書かせる。

番でないのに say:

```json
{
  "ok": false, "error": "not_your_turn",
  "turn": "frontend", "next": "receive",
  "hint": "今は相手の発言待ちです。receive を呼んでください。"
}
```

#### advanced の申告基準

自己申告は楽観的に倒れやすく、要約しただけでも「論点を整理したので前進した」と判断されうる。
SKILL.md には抽象的な基準ではなく具体例を書く。

> 相手への同意・お礼・要約・確認だけの発言は `--advanced false`。

---

### receive

| 状況 | status | next |
|---|---|---|
| 新着あり | `message` | `say` |
| 時間切れ・リトライ残あり | `timeout` | `receive` |
| 会話中に残なし | `no_answer` | `ask_user` |
| 参加待ちで残なし | `closed` (`no_response`) | `report` |
| 自分の番だった | `your_turn` | `say` |
| 相手が閉じた | `closed` | `report` |

相手がまだ参加していない間は待つ。参加者が 2 人に満たないルームでは、
自分に発言権があっても `your_turn` を返さない。開いた直後に呼んでも待機に入り、
相手が `join` した時点で返る。

参加を待っている間の時間切れは、会話中の長考とは別に数える。
room_id が伝わるまでの時間は相手の思考時間ではないため、
同じ回数に含めると会話が始まる前にリトライを使い切る。
時間切れの hint も「相手がまだ参加していません」とし、
停止か長考かを区別できないという会話中の説明を流用しない。
往復の上限に達して閉じたとき、最後の発言は相手にとって未読のまま残る。
ここで未読を捨てると、相手の最終見解を読まないまま要約することになる。

新着:

```json
{
  "ok": true, "status": "message", "room_id": "r-a3f9",
  "messages": [ { "seq": 4, "from": "frontend", "at": "...", "text": "..." } ],
  "hops_left": 3, "turn": "backend", "next": "say"
}
```

どのルームの応答かを応答だけで判別できるよう、`room_id` を必ず返す。

タイムアウト:

```json
{
  "ok": true, "status": "timeout", "waited_seconds": 100, "retries_left": 4,
  "next": "receive",
  "hint": "まだ応答がありません。相手が考え中の可能性が高いです（長考は珍しくありません）。receive を再度呼んでください。あと 4 回待てます。"
}
```

`retries_left` を明示する。残り回数が示されないと早期に待機を打ち切る判断がなされやすい。

自分の番だった場合は即座に返す。デッドロック回避の要。

```json
{ "ok": true, "status": "your_turn", "next": "say",
  "hint": "あなたの発言番です。待たずに say を呼んでください。" }
```

room_id を 2 つ以上渡した場合は `ok: false` で断る。
複数を受け付けて先頭だけを見る実装にはしない。待てているつもりで
もう一方を取りこぼす形になり、誤った説明が付く。

#### タイムアウトを 100 秒とする理由

呼び出し側の Bash 実行には既定のタイムアウト（120 秒）があり、
`receive` の待機がこれと競り合うと不可解な失敗になる。
100 秒であれば呼び出し側で何も指定しなくても既定値の内側に収まる。
長考への追随は 1 回の待機時間ではなくリトライ回数で確保する。

リトライ上限は 9 回とする。100 秒の待機と合わせて、まとめて待てる長さは 900 秒になる。

#### 会話が始まっていれば、上限に達しても閉じない

相手が停止したのか書いている最中なのかは、**このツールでは検出できない。**
書いている側は書き終えるまで何も書き込まないため、生きている証拠がディスクに現れない。
`receive` の時間切れが記録するのは、待っている側が待っていたという事実だけである。

閉じた場合の代償は未読が残ることではない。`say` は閉じたルームを入口で断るため、
上限の向こう側で書かれていた本文は**ディスクに存在しないまま失われる**。

検出できないものを判定に使うのをやめ、判定を人間に返す。上限に達したら
`no_answer` を返して `participants[].timeouts` を 0 に戻し、ルームは開いたままにする。
相手のセッションが動いているかを確かめられるのは、その画面を見られる人間だけである。

歯止めは新たに設けない。`receive` の待機は 1 回ごとにアイドル掃除を通り、
`last_activity_at` は時間切れでは更新されないため、
最後の発言から 30 分でルームは `idle` として閉じる。

**参加を待っている間は従来どおり `no_response` で close する。** 相手が来ていなければ
発言も書かれておらず、閉じることで失われるものが無い。

#### 掃除は「書いている最中」を巻き込む。壊れない形にする

`no_answer` は 15 分の側だけを人間に返した。**30 分の側は今までどおりツールが決める。**
そして掃除で閉じたルームへの `say` は入口で断られるため、
書き上げるのに 30 分を超えた本文は、書いた本人の `say` が閉じにいったうえで失われる。

**掃除を走らせているのは当事者とは限らない。** `check` は参加者を見る前に掃除を通し、
Stop hook として全セッションのターン終了で走る。同じマシンで cxtalk を使う
セッションが 1 つでも動いていれば、無関係なルームもそこで閉じられる。
**主要な閉じ手は第三者であり、当事者側の経路を守っても届かない。**

そこで 2 つに分けて扱う。

- **行為は参加の証拠として扱う。** `join` と `say` では掃除を通さない。
  過去の無音を理由に、いま来た参加や発言を断らない。
  第三者が先に閉じていれば同じ結果になるため、これだけでは足りない
- **断っても本文は残す。** 閉じたルームへの `say` は、`closed-` で始まる名前で
  `messages/` に書く。数字で始まらないため発言として数えられず、通し番号の検査にも入らない。
  既にある `ignored` の窓から人間に届く

**残る線引きは人間が引く。** 本文は残るが会話は終わっており、相手はそれを読んで返事ができない。
そこまで守るには掃除も人間に返すことになるが、掃除の対象は誰も見ていないルームであり、
返す相手がいない。値を選ぶのではなく、**壊れたときに何が残るか**を決める問題として扱う。

**時間切れを記録する前に、これから閉じにいく状態そのものを見る。**
待機の途中で相手が閉じることがあり、そのときの理由は `hop_limit` や `manual` になる。
待っていた側が `no_response` と決め打つと、相手が話し切った会話に
応答が無かったという記録が残る。未読が漏れるだけなら読み直せるが、
応答が無かったという理由が付いた時点で読み直す動機そのものが消える。
閉じられていたと分かった時点で、未読を渡して実際の理由を返す。

判定は、閉じるときに書き込む写しに対して行う。別に読んだ状態で判定すると、
判定と書き込みが違うものを見ることになり、確かめたはずの経路が守りにならない。

ただしこれで競合が消えるわけではない。読み込みが返すのはその時点の写しであり、
判定を通った後に相手が閉じれば、こちらの写しがそれを上書きする。
同じ写しを見ていることと、同じ状態を見ていることは別である。

数えるのは一続きの待機に限る。相手の発言が届いた時点で 0 に戻す。
時間切れは往復のたびに起きるため、会話を通して数え続けると、
応答している相手との会話が途中で `no_answer` に着地し、
答えている相手を止まっているかもしれない相手として人間に上げることになる。

時間切れは例外ではなく通常の経路である。長文を書く相手は 100 秒を普通に超えるため、
待機のたびに一定の割合で発生する。`no_answer` の hint には、相手が停止したのか
まだ書いているのかが分からないことを含める。判定しない以上、断定して報告させない。

---

### status

**既読にしない。** `last_read` を更新しない。
更新すると、確認しただけで既読になり取りこぼしを生む。

無音のまま放置されたルームの掃除は通す。掃除を通さないと、実際には閉じているルームを
`open` として説明し、その `next` に従った次のコマンドで `closed` が返る。

参加していない名前にも状態を返すが、`next` は `ask_user` とする。
`say` や `receive` を促しても、その次のコマンドで断られる。
閉じたルームでも `report` は返さない。参加していない相手に要約と原文の場所を渡さない。

```json
{
  "ok": true, "room_id": "r-a3f9", "status": "open", "closed_reason": null,
  "topic": "...", "turn": "frontend",
  "max_hops": 5, "hops_used": 3, "hops_left": 2,
  "unread": { "backend": 0, "frontend": 1 },
  "retries_left": 9,
  "ignored": [], "unknown_keys": [],
  "participants": ["backend", "frontend"],
  "last_activity_at": "2026-08-07T14:32:10+09:00"
}
```

**`room.json` に既にある値は出す。** 出さない値は、呼ぶ側が状態ファイルを直接読むか、
副作用のあるコマンドを呼ぶかでしか知れなくなる。`retries_left` がこれに当たっていた。
待機の残りを知る手段が時間切れの応答だけだと、残りを知るために残りを 1 使うことになる。

`retries_left` は参加していない名前には `null` を返す。0 を返すと、
予算を持たないことと使い切ったことが同じ値になる。

---

### ls

閉じたものを含めてすべてのルームを返す。終わった会話の原文へ戻る導線を切らないため。
開いているかどうかは各行の `status` で判別する。

読み取れないルームは `rooms` に出せないため、`unreadable` に room_id を並べる。
黙って一覧から落とすと、壊れていること自体がどこにも現れない。

`status` と同じく掃除を通す。一覧と個別の状態が食い違う説明を作らない。

各行は `closed_reason` `max_hops` `hops_used` を含める。`next` が `ask_user` である以上、
選ぶのは人間であり、選べるだけの材料が要る。閉じた理由と進み具合が出ないと、
話し切った会話と一言も交わさずに閉じたルームが同じ `closed` にしか見えない。

---

### close

```json
{
  "ok": true, "closed_reason": "manual", "hops_used": 3,
  "next": "report",
  "hint": "ルームを閉じました。ここまでの内容を要約してユーザーに報告してください。"
}
```

`--reason` は終了条件の表にある 5 値だけを受ける。
`closed_reason` は報告の出力にそのまま乗るため、
定義されていない文字列が人間の目に届くことを避ける。

掃除を通してから閉じる。既に閉じているルームの理由は上書きせず、そのまま返す。
掃除を通さないと、無音で死んでいたルームに `manual` が付く。

**閉じるときに `last_activity_at` を打ち直さない。** 閉じることは活動ではない。
掃除は無音の長さで判定するのに、その判定の結果で判定の入力を上書きすることになり、
掃除が走った時刻が最後の発言の時刻として残る。無音がいつ始まったかは
発言のファイルからしか復元できなくなり、フィールド名と中身が食い違う。
掃除が見るのは開いているルームだけなので、打ち直さなくても判定は変わらない。

---

### check

人間が介入する窓と、hook から呼ぶ判定に使う。他のサブコマンドと異なり、
JSON ではなく終了コードと 1 行のテキストで答える。

| exit | 意味 |
|---|---|
| 0 | 用がある（自分の turn ∧ 未読あり）。stdout に 1 行 |
| 1 | 用がない |
| 2 | 判定できない（状態を読めない等） |

```
$ cxtalk check --as backend
r-a3f9: frontend から 1 件（残り2往復）— 確認テストの合格基準は正答数か正答率か
$ echo $?
0
```

読み取れないルームは飛ばして残りを判定する。1 件の破損で全件が失われると、
未読があること自体が伝わらなくなる。読み取れないルームの存在は `ls` が伝える。

exit 2 は hook 側で「用なし」として扱う。判定できない状態で恒久的にブロックされると
復帰できないため、安全側に倒す。

stdout の 1 行はそのまま hook の `reason` に流せる形式にする。hook 側でのパースを不要にする。

## 終了条件

| closed_reason | トリガー | 着地 |
|---|---|---|
| `hop_limit` | 往復が `max_hops` に到達 | 要約して人間へ |
| `stale` | `advanced: false` が 2 連続 | 要約して人間へ |
| `no_response` | 参加を待つリトライの上限 | 相手が参加しなかった旨を人間へ |
| `idle` | 30 分無音 | 掃除（要約なし） |
| `manual` | 人間または一方が明示的に close | 要約して人間へ |

会話中の待機が上限に達しても閉じない。`no_answer` を返して人間に判断を返し、
決着が付かないまま放置されたルームは `idle` が拾う。
`no_response` は自動では会話中に書かれなくなるが、`close --reason` では受け続ける。
既に書かれているものの意味は変わらないため、名前は変えない。

前 2 者が正常終了、残りが異常系。すべての終了が `next: "report"` に収束する。
このプロトコルには「会話が終わったら要約して人間に返す」以外の出口がない。

**閉じたルームの理由は上書きしない。** 後から閉じにきた側の理由で書き換えると、
話し切った会話に応答が無かったという記録が付く。
閉じる操作そのものが既に閉じているかを見て、呼び出し側の守りに頼らない。
呼び出し側に置いた守りは、隣の変更で外れる。

この不変条件が守るのは、同じ写しを扱う経路に限られる。掃除が閉じた直後に
`close` が続くような、1 つの呼び出しの中で起きるものはここで止まる。
別の呼び出しが閉じた場合は写しに現れないため、上の競合として残る。

`hop_limit` の判定には、発言を促す hint が終端を予告するのと同じ条件を使う。
式を分けると、予告のないまま閉じる形にも、最後ではない発言に最後だと告げる形にもずれる。
どちらも「最後の発言で押し切らない」という取り決めを裏切る。

## Stop hook 連携

`receive` はターンの内側でしか待てない。会話が終わったと判断して人間に応答を返した後は、
相手が発言しても届かない。この取りこぼしを Stop hook が拾う。

```
ターンを終える
  → Stop hook が cxtalk check を叩く
  → exit 0 なら block
  → 再入場して会話を続ける
```

block する条件は **自分の turn ∧ 未読あり ∧ `stop_hook_active` が false** の 3 つすべて。

閉じたルームも対象に含める。上限に達して閉じたとき最後の発言は相手にとって未読のまま残り、
通知しなければ読まれずに終わる。読めば既読になり次の判定を通らないため、
閉じたルームで block しても止まり続けることはない。
条件から未読を外すと抜けられなくなるので、未読を必ず含める。

`stop_hook_active` は Stop hook の入力に含まれ、block によって継続したターンで true になる。
Claude Code は連続した block を検知しないため、これを見ない限り止まらない。
このフラグは人間の入力ごとに false へ戻るため、条件に加えることで
block は入力 1 回につき最大 1 回に制限される。

**入力の解釈は hook スクリプトではなく `check` が行う。** `--hook true` を渡すと
標準入力の JSON を読み、`stop_hook_active` を値として見る。
文字列の一致で判断すると、本文に同じ字面があるだけで空振りし、
しかも空振りは block を続ける側へ倒れる。倒れる向きが逆のものを同じファイルに並べない。

読み取れない入力は判断できないものとして扱い、block しない側へ倒す。
止め続けると会話から抜けられないためで、`check` が 2 を返すときの扱いと同じである。

block 時の表示には残り往復数を含める。人間が画面を見ていれば中断できる状態にする。
閉じたルームに残りの往復はないため、往復数ではなく閉じている旨を出す。
続けられない会話を継続中の会話と同じ形で見せると、中断すべきかの判断を誤らせる。

## 歯止め

自走する以上、人間の不在中に往復が進む。ルームの位置づけを次のように定める。

**ルームは論点を詰める場であり、決定する場ではない。** 成果物は「決定」ではなく
「整理された論点と、双方の見解」とする。決定は人間が行う。

3 つの歯止めを置く:

- `max_hops` の既定値を 5 とする
- 終端の `hint` に「結論は提案であり、実装はユーザーの承認を経ること」を含める
- Stop hook の block 時に残り往復数を表示する

歯止めを SKILL.md ではなくコマンドの出力側に置くのは、skill を読んでいない状態でも効かせるため。

## 実装

| 項目 | 決定 |
|---|---|
| 言語 | TypeScript |
| 実行 | Node 24 の type stripping により、ビルドせず `.ts` を直接実行 |
| 依存 | なし（標準ライブラリのみ。`node_modules` を持たない） |
| 構成 | 1 ファイル |
| 呼び出し | Bash ツールの `PATH` から `cxtalk` として実行。hook からも同じ |

型注釈を除去して実行する方式のため、ランタイム挙動を伴う構文（`enum` / `namespace` /
パラメータプロパティ）は使えない。union type と const オブジェクトで代用する。
型検査は実行時には行われないため、エディタまたは別途の `tsc` に委ねる。

終了コードは `process.exitCode` への代入で設定し、`process.exit()` は呼ばない。
Windows の Node 24 では、型注釈を除去した `.ts` が `fs` の同期 API と `process.exit()` を
併用するとプロセスが libuv のアサーション失敗で異常終了し、終了コードが 127 になる。
`check` は終了コードで結果を表すため、これを踏むと判定が成立しない。
`process.exit()` は保留中の標準出力の書き込みも切り捨てるため、いずれにせよ使わない。

`.ts` の直接実行は stderr に ExperimentalWarning を出力する。
`check` の出力を hook に渡す際のノイズになるため、`bin/cxtalk` から
`--disable-warning=ExperimentalWarning` を渡して抑止する。

`package.json` は `{"type": "module"}` のみを持つ。これがないと import を含む `.ts` の
実行時に MODULE_TYPELESS_PACKAGE_JSON の警告が stderr に出力され、同じくノイズになる。
依存は持たないため `node_modules` は生じない。

### 呼び出しの前提

`bin/` に置いたものはプラグインが有効な間 Bash ツールの `PATH` に加わるため、
`cxtalk` の名前で呼べる。ただし**実行許可を settings に登録する**必要がある。
登録がないと往復のたびに許可を求められ、自走が成立しない。

### テスト

`node --test` で実行する。`node:test` と `node:assert` のみを使い、追加の依存を持たない。

コマンドを子プロセスとして起動し、標準出力の JSON と終了コードを検証する。
出力の形が仕様であるため、内部の関数ではなく外から見た振る舞いを対象にする。
`CXTALK_HOME` に一時ディレクトリを渡し、実データから隔離する。

**利用者が叩く入口と Stop hook も同じ形で検証する。** `bin/cxtalk` を経由せず
`src/cxtalk.ts` を直接起動すると、ラッパーと hook が検証の外に残る。
JSON の出力を対象にする検証をいくら増やしても、その手前と奥には届かない。

**出力に現れない値は、現れる経路を通して固定する。** 待機の既定値は時間切れの出力にしか
現れないため、既定のまま 1 回待つ検証を置く。実時間を要するが、
呼び出し側の制限を超えたときに起きるのは原因の分かりにくい失敗であり、散文では守れない。

## skill との分担

| | 担当 |
|---|---|
| コマンド | 機構。メッセージの保存・待機・カウント。決定論的な部分 |
| SKILL.md | 作法。いつ開くか、何を載せるか、どう閉じるか。判断を要する部分 |

SKILL.md とコマンド本体は同じプラグインに同梱する。
repo ごとに複製すると片方だけが更新され食い違うため。

## プラグインとしての構成

Claude Code のプラグインとして構成する。skill・hook・実行ファイルの置き場所が
仕様として定まっており、配置とパス解決を自前で用意せずに済む。

```
.claude-plugin/plugin.json
bin/cxtalk
src/cxtalk.ts
skills/cxtalk/SKILL.md
hooks/hooks.json
hooks/stop.sh
```

| 置き場所 | 役割 |
|---|---|
| `bin/` | プラグインが有効な間、Bash ツールの `PATH` に加わる |
| `skills/cxtalk/SKILL.md` | 作法 |
| `hooks/hooks.json` | Stop hook の登録。利用者が settings を編集せずに済む |

`bin/cxtalk` は `src/cxtalk.ts` を起動するラッパーとする。
型注釈の除去は拡張子で判定されるため、拡張子を持たない `bin/cxtalk` に実装を直接は置けない。

シェルがラッパーを起動するとき、インタプリタへ渡すのはスクリプト自身のパスであり、
その形は呼び方で決まる。`PATH` から解決された場合は絶対パスになる。
シェルの名前空間と node の名前空間が異なる環境では、この絶対パスを node が開けないため、
変換器が決まった場所にあればそれを通す。

```bash
if [ -x /usr/bin/cygpath ]; then
  root="$(/usr/bin/cygpath -m "$root")"
fi
```

**変換器を `PATH` から探してはならない。** 同名のものが複数の環境に同居し、
それぞれ自分の名前空間しか正しく直せない。`PATH` の順で拾うと、
別の名前空間の規則で解決されたパスが返り、存在しない場所を指す。
置き場所を指定して呼ぶことで、シェルごとに自分のものが使われる。

**この形は自動テストで完全には守られていない。** シェルの名前空間の絶対パスには
複数の綴りがあり、テストが自分で作れるのはそのうち 1 つだけである。
残りを正しく直せるかは、その名前空間を持つシェルでしか確かめられない。
変換の形を替えるときは、テストが通ることを根拠にしない。

### 配置

`~/.claude/skills/cxtalk` を本 repo への symlink とする。
`.claude-plugin/plugin.json` を持つディレクトリはプラグインとして読まれ、
プラグインキャッシュに複製されずその場で参照されるため、repo が唯一の正になる。
marketplace への登録も install も要さず、次のセッションから `cxtalk@skills-dir` として読まれる。

開発中は `claude --plugin-dir <path>` で読み込み、`claude plugin validate <path>` で検証する。

配布する場合に備え、`.claude-plugin/marketplace.json` でこの repo 自身を marketplace として
定義している。marketplace 経由で install するとプラグインキャッシュへ複製されるため、
repo をその場で読む symlink とは変更の反映のされ方が異なる。

## 初期実装に含めないもの

- **発言の繰り返し検知**（類似度計算による堂々巡りの検出）。実装量に対して閾値調整の見通しが立たない。
  `max_hops` と `stale_streak` で実際にどこまで止まるかを見てから判断する
- **3 者以上のルーム**。`turn` を 2 者の交代として扱っており、発言権の巡り方から決め直す必要がある
- **複数ルームの同時待機**。`receive` は room_id を 1 つだけ受ける。
  待つ順序、hops とリトライの数え方がいずれも未設計であり、
  半分だけ動く経路を残すより断る方がよい
- **上限を変えるコマンド**。`max_hops` は会話を止めるための歯止めであり、
  止める対象は会話する側にある。会話する側が呼べるコマンドで変えられるようにすると、
  歯止めが自己申告になる。話が続いているときの延長は当事者には常にもっともらしく見える

## 上限を変えるとき

開いた後に `max_hops` を変える手段はコマンドとして持たない。
必要になったら人間が `room.json` を直接書き換える。手間があること自体が、
延長の判断に人間を経由させる仕組みとして働く。

`room.json` に残るのは、失われても会話が続くものだけである。
発言権と往復数は通し番号から導くため、状態が古くても会話の筋は狂わない。

ただし書き換えを誤った場合は、その値を使う前に断る。黙って既定へ寄せると、
歯止めや先手がこちらの決めた値に変わり、人間が書いたつもりの条件と
食い違ったまま会話が進む。断られたルームは人間が直すまで開いたままになる。

書き換えたことは会話の相手からは理由が読めない。
条件が変わったことを原文に残すため、書き換えた側は `say` で理由を 1 件書く。

## 不具合を並べる基準

複数の不具合を前にして直す順序を決めるとき、復帰できるかどうかではなく
**誤った説明が付くか**を第一に見る。

誤った説明が付く不具合は調査が始まらない。相手が黙っているようにしか見えない、
応答がなかったという筋の通った記録が残る、といった形で正常な経過に見えてしまうと、
被害の大小に関わらず発見までの時間が伸びる。
プロセスが戻らないような明白な異常は、取り返しがつかなくても、
気づいた時点で正しい方向を向いている。

この基準の対象は動作の誤りに限る。性能の劣化は誰も誤解しないまま遅くなり、
秘密の漏洩は気づかれないこと自体が被害になる。いずれも別の規則で並べる。
範囲を書かずに第一の基準と宣言すると、別種の欠陥を後回しにする根拠に使われる。
